//filter() retorno o elemento que atende as condições em um novo array

const age = ["12", "56", "14", 5, "32", "69", 45]

const ages = (elx) => typeof el === "number"

const newArr = age.filter(ages)

console.log(age)
console.log(newArr)